// # this is a just copy to use it later.
// function getCPU() {
//   exec('top -bn2 | grep "Cpu(s)"', (error, stdout, stderr) => {
//     if (error) {
//       console.error(`exec error: ${error}`);
//       return;
//     }
//     return stdout;
//   });
// }
