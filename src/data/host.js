function host(name, osVersion, kernelVersion, dockerContainers, usage) {
  this.name = name;
  this.osVersion = osVersion;
  this.kernelVersionn = kernelVersion;
  this.dockerContainers = dockerContainers;
  this.usage = usage;
}
